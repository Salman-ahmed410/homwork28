import datetime

# Generate a list of month names using list comprehension
months = [datetime.date(2000, m, 1).strftime('%B') for m in range(1, 13)]

# Display the months
for month in months:
    print(month)
